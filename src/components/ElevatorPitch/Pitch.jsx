import React from 'react';

const Pitch = () => {
    return (
        <section className="pitch__container section">
          <h2 className="section__title">Elevator pitch</h2>
            <span className="section__subtitle">Video</span>

        </section>
    );
};

export default Pitch;